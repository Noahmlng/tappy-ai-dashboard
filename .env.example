# Dashboard (.env.example)

# Required in production (Control Plane API origin; /api suffix optional)
MEDIATION_CONTROL_PLANE_API_BASE_URL=https://control-plane.example.com/api

# Optional for local development only
MEDIATION_CONTROL_PLANE_API_PROXY_TARGET=http://127.0.0.1:3100

# Optional runtime domain verification config (default: runtime-gateway.tappy.ai)
MEDIATION_RUNTIME_GATEWAY_HOST=runtime-gateway.tappy.ai

# Optional strict mode: require customer domain CNAME to runtime gateway (0/1)
MEDIATION_RUNTIME_REQUIRE_GATEWAY_CNAME=0

# Optional managed runtime base; if unset, proxy derives from MEDIATION_CONTROL_PLANE_API_BASE_URL
# MEDIATION_MANAGED_RUNTIME_BASE_URL=https://runtime-managed.example.com
